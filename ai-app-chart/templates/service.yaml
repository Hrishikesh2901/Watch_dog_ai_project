apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-app-chart.fullname" . }}
  labels:
    {{- include "ai-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-app-chart.selectorLabels" . | nindent 4 }}
